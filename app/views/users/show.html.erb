<div class="row">
    <div class="col"></div>
        <div class="col-4">
            <%= @user.name %>
        </div>
    <div class="col"></div>
</div>

<div class="row">
    <div class="col"></div>
        <div class="col-4">
            <% @user.events.each do |event| %>
                <%= event.description %>
                <%= event.location %>
                <%= event.date %>
            <% end %>
        </div>
    <div class="col"></div>
</div>