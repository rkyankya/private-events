<nav>
  <% if logged_in? %>
    <%= link_to  "#{@current_user.name}", @user, class: 'btn btn-secondary' %>
    <%= link_to  "Log out", logout_path, method: :delete, class: 'btn btn-secondary' %>
  <% else %>
    <%= link_to  "Sign in", login_path, class: 'btn btn-primary' %>
  <% end %>
</nav>